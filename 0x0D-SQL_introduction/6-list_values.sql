-- lists all rows of the table first_table from the databse hbtn_0c_0 in your Mysql server
-- All fields should be printed
-- The database name will be passed as the mysql command

SELECT * FROM first_table;
