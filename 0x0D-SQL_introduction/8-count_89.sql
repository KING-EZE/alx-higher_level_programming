-- displays the number of records with id = 89 in the table first_table of the database mysql server
-- The database namw will be passed an argument of the mysql command

SELECT COUNT(*) FROM first_table WHERE id=89;
